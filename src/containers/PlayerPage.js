import React, { Component } from 'react';
import { connect} from 'react-redux';

class PlayerPage extends Component {
    render() {
        return (
            <section className="player-page"
                
            ></section>
        )
    }
}

export default connect()(PlayerPage);